import UserModel from './user.models.js';
import ChanelModel from './chanel.models.js';

const User = UserModel;
const Chanel = ChanelModel;

const models = {
  User,
  Chanel,
};

export default models;
